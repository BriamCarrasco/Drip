name: drip

services:
  app:
    build: .
    image: drip
    container_name: drip
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
